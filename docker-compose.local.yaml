# 本地/内网测试环境配置(无 SSL)
version: '3.8'

services:
  sanmoo-server-go:
    environment: {}

  sanmoo-vite:
    environment:
      NGINX_ENV: local  # 覆盖为 local 环境
    ports:
      - "80:80"
      # 不暴露 443 端口,因为本地不需要 HTTPS
